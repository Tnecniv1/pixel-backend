export const theme = {
  colors: {
    bg: "#18162A",         // Fond principal sombre
    card: "#0F0E20",       // Fond des cartes et panneaux
    text: "#E0E0E0",       // Texte principal clair
    border: "#2A2940",     // Contours discrets
    accent: "#FFD93D",     // Jaune pixel (boutons principaux)
    secondary: "#4DB7FF",  // Bleu pixel (liens, progression)
    danger: "#FF4D4D",     // Rouge pixel (erreurs)
  },
  spacing: (n: number) => 8 * n,
};
